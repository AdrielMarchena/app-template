#pragma once

/*
* This header contains class enums and declarations
* for functions to translate this enums for GL specific macros
*/

namespace Game
{
	struct gl
	{
		enum class TextureTypes
		{};

		enum class DrawType
		{};

	};
}